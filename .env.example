# Vite client-side variables (compiled into the browser bundle).
# Copy to `.env` and fill with your real values.
#
# These are public — do NOT put SECRET keys here.  Publishable key only.
VITE_SUPABASE_URL="https://YOUR-PROJECT-REF.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_publishable_xxxxxxxxxxxxxxxxxxxxxxx"
